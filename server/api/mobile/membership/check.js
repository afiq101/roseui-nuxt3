import { initial_api_status } from "~~/server/constant"

export default defineEventHandler(async(event) => {

    let response = initial_api_status;

    let user_id                 = null;
    let user_secret_key         = null;
    let user_membership_code    = null;

    try {

    } catch(e) {

    }

    return response;
})